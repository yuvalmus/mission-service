
כדי למנוע מורכבות של עצי פיצולים, אירועי מיזוג (Merge) ושאילתות מורכבות בזמן נתק רשת, המערכת פועלת במודל **Last-Write-Wins (LWW)** מבוסס טבלה שטוחה, בשילוב מנגנון גיבוי היסטורי אוטומטי (`route_backups`) ברמת בסיס הנתונים.

### 1. מודל הנתונים וזרימת ה-LWW

- **תמיד נתיב יחיד פעיל:** טבלת היישויות/נתיבים הראשיים (`entities` / `routes`) מחזיקה **רק את המצב העדכני ביותר** של הנתיב.
    
- **עבודה בנתק וסנכרון (`pglogical`):** בזמן נתק רשת, כל רכב/צומת ממשיך לעדכן את המצב המקומי. בעת החזרה מנתק והסנכרון, המידע העדכני ביותר דורס את המצב הקיים ב-DB לפי מודל **LWW מבוסס שעון (`last_update_time`)**. כיוון שהמערכת פועלת בסביבת Standalone בסשנים מוגדרים (עד 12 שעות) ומונעת מפעולות משתמש, סטיות זמנים זניחות ומתקבלות.
    
- **אפס אובדן מידע:** המצב שהיה קיים בטבלה רגע לפני ה-`UPDATE` אינו נמחק, אלא מגובה אוטומטית לטבלת היסטוריה מיועדת באמצעות Trigger.
- **טיפול במחיקה רכה (Soft Delete):**

- מחיקת יישות אינה מבצעת פקודת `DELETE` פיזית בטבלה, אלא פקודת `UPDATE entities SET isDeleted = true ...`.
    
- **בטבלת הגיבויים (`route_backups`):** המצב שהיה קיים ביישות **רגע לפני המחיקה** מגובה אוטומטית כגרסה קודמת דרך טריגר ה-`BEFORE UPDATE` הקיים.
    
- **שחזור ממחיקה (Restore):** במידה ומבצעים שחזור (Restore) או ביטול מחיקה, מריצים פקודת `UPDATE` שמחזירה את `isDeleted = false`. פעולה זו שוב נחשבת כעדכון רגיל ומייצרת גרסה חדשה בגיבוי.

- **עדכון לקוחות ו-Delta Sync:** ישויות שנמחקו רכה מוחזרות כחלק משאילתת ה-Delta כשהן מסומנות ב-`isDeleted = true`, ומאפשרות ללקוח ה-Frontend להסיר או לעדכן את היישות במפה.
    
### 2. טבלת הגיבויים (`route_backups`)

טבלה זו מרכזת את כל גרסאות העבר של הישויות/נתיבים. כדי לשמור על גמישות מלאה ולמנוע כפילויות תחזוקה, **הסכמה של טבלת הגיבויים זהה לחלוטין לסכמת טבלת הישויות (`entities`) בתוספת מזהה אירוע ייחודי (`event_id`)**:

- **event_id** (`uuid`): מזהה ייחודי עבור רשומת הגיבוי (Primary Key).
- **כל שאר השדות:** מועתקים במלואם ישירות מטבלת הישויות (`id`, `mission_id`, `infra_id`, `version`, `mission_change_seq`, `geometry`, `properties`, `is_deleted`, `last_update_time`, וכו').

אינדקסים מומלצים:
```sql
-- אינדקס לשליפה מהירה של היסטוריית ישות ב-UI (לפי מזהה ישות וגרסה)
CREATE INDEX idx_route_backups_entity_ver 
ON route_backups (id, version DESC);

-- אינדקס המאפשר שליפה היסטורית / שחזור ברמת משימה (Time Travel / Undo)
CREATE INDEX idx_route_backups_mission_seq 
ON route_backups (mission_id, mission_change_seq DESC);
```

### 3. מנגנון הטריגר וסנכרון הגיבויים (`trg_route_lww_backup`)

#### מניעת בעיית השכפול הכפול (Replication Duplication Problem)
כדי שכל המחשבים ברשת ה-Mesh יחלקו את כל היסטוריית הגרסאות ויוכלו לשחזר נתונים שנוצרו בכל מחשב, טבלת `route_backups` עצמה **משוכפלת בין המחשבים** דרך `pglogical`. 

אולם, שילוב של שכפול טבלת הגיבויים יחד עם טריגר `BEFORE UPDATE` טומן בחובו סכנת **שכפול כפול בריבוע**:
- כאשר מפעיל במחשב א' מעדכן נתיב, הטריגר במחשב א' מייצר שורת גיבוי עם `event_id` משלה ומעדכן את הישות.
- שני השינויים (הנתיב המעודכן ושורת הגיבוי החדשה) נשלחים דרך `pglogical` למחשב ב'.
- **נקודת הכשל הפוטנציאלית:** אם הטריגר במחשב ב' יופעל שוב כתוצאה מקבלת העדכון מהרשת, הוא ייצר **שוב** שורת גיבוי נוספת עבור אותו שינוי (עם `event_id` חדש). התוצאה: היסטוריית הגיבויים תוכפל ותוצף ברשומות כפולות עבור כל עריכה בודדת!

#### הפתרון: הפעלת הטריגר במקור בלבד (`Origin Only`)
העיקרון המנחה הוא: **"מי שמבצע את העריכה המקורית – הוא ורק הוא מייצר את שורת הגיבוי ומקדם את מונה המשימה. שאר המחשבים רק קולטים את העדכון ואת שורת הגיבוי שהגיעה ברפליקציה."**

לשם כך, פונקציית הטריגר בודקת את משתנה המערכת `session_replication_role`:
- **בעריכה מקומית רגילה (`session_replication_role <> 'replica'`):** הטריגר מכניס את ה-`OLD` ל-`route_backups`, מקדם את מונה המשימה (`missions.last_change_seq`) ומעלה את גרסת הישות. שורת הגיבוי והישות משתכפלות יחד לשאר המחשבים.
- **בעת קבלת רפליקציה מ-`pglogical` (`session_replication_role = 'replica'`):** הטריגר מדלג על יצירת הגיבוי ועל קידום המונה, כיוון ששורת הגיבוי המקורית והשדות המעודכנים כבר משוכפלים ישירות מ-pglogical.

SQL

```sql
CREATE OR REPLACE FUNCTION trg_route_lww_backup()
RETURNS TRIGGER AS $$
DECLARE
    v_next_seq BIGINT;
BEGIN
    -- 1. הפעלה בעריכה מקומית בלבד (מונע שכפול כפול של שורות גיבוי ברפליקציה)
    IF (session_replication_role <> 'replica') THEN
        -- שמירת השורה הישנה במלואה (OLD.*) לתוך טבלת הגיבויים בתוספת event_id חדש
        IF (TG_OP = 'UPDATE') THEN
            INSERT INTO route_backups
            SELECT gen_random_uuid() AS event_id, (OLD).*;
        END IF;

        -- קידום אטומי של מונה השינויים ברמת המשימה (עם נעילת Row Lock קצרה)
        UPDATE missions
        SET last_change_seq = COALESCE(last_change_seq, 0) + 1
        WHERE id = NEW.mission_id
        RETURNING last_change_seq INTO v_next_seq;

        -- עדכון השדות ביישות
        NEW.version := COALESCE(OLD.version, 0) + 1;
        NEW.mission_change_seq := v_next_seq;
        NEW.last_update_time := NOW();
    END IF;

    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- הצמדת הטריגר לטבלת הישויות
CREATE TRIGGER trg_routes_lww_backup
BEFORE INSERT OR UPDATE ON entities
FOR EACH ROW
EXECUTE FUNCTION trg_route_lww_backup();
```

**אופטימיזציה לביצועים (Decoupled Pruning):**

- **בתוך הטריגר (`BEFORE UPDATE`):** הטריגר מורשה לבצע **שמירה בלבד (`INSERT`)** של השורה הישנה אל `route_backups` וקידום המונים.
    
- **מחוץ לטריגר (Background Cleanup Job):** ניקוי גרסאות מעבר לתקרת 10 הגרסאות (`DELETE`) הוסר מפקודת הטריגר המיידית והועבר לתהליך רקע תקופתי (כגון `pg_cron` או Worker). הפרדה זו מונעת עיכובי I/O ונעילות מיותרות בתוך הטרנזקציות של האפליקציה.

### 4. פונקציונליות UI (קליק ימני)

הודות לטבלת `route_backups`, ה-UI מקבל יכולות ניהול היסטוריה פשוטות וישירות:

- **צפייה בהיסטוריה (Audit / History):** קריאת REST/Query פשוטה לפי `route_id` השולפת את 10 השורות מ-`route_backups` ומציגה למפעיל את שינויי העבר של הנתיב.
    
- **שחזור גרסה (Restore Version):** לחיצה על "שחזר לגרסה זו" מריצה פקודת `UPDATE` רגילה על הנתיב הראשי עם ה-`geometry` וה-`properties` של הגיבוי הנבחר. המצב שנדרס נכנס שוב לגיבוי כגרסה חדשה.
    
- **שכפול מבוסס גרסה ישנה (Duplicate as New):** לחיצה על "צור נתיב חדש מגרסה זו" מריצה פקודת `INSERT` לטבלת `entities` עם הנתונים מהגיבוי ו-`id` חדש לגמרי.

### 5. קונפליקטים בנתיב בחזרה מנתק (הזזה ומחיקת נקודות)

ארכיטקטורת הגיבוי וה-LWW מתמודדת עם קונפליקטים מורכבים ברמת הנתיב (Routes):
- **הזזת נקודות בנתק:** כאשר נקודת ניווט מוזזת על ידי מחשב א' בזמן נתק, ומחשב ב' עורך את הנתיב, חזרת הסנכרון (Catch-up) תגרום לנקודה החדשה להזיז את הנתיב בהתאם. במקרה שהדריסה של ה-LWW דרסה מידע חשוב (הנתיב של מחשב ב' דרס את הנתיב של מחשב א'), מנגנון טבלת הגיבויים שנסנכרה מאפשר לראות את הגיבוי של מחשב א' ולבצע Undo אם נדרש.
- **מחיקת נקודות:** במקרה של חזרה מנתק, נתיב מחשב א' (שהיה בנתק) עלול להחזיר לחיים נקודות שמחשב ב' מחק. זהו מצב תקין (By Design). חובה לוודא שה-UI והשרת מתייחסים לנקודות אלו כאל נקודות ניווט לגיטימיות (למשל מסוג `LinePoint`). יש להקפיד שמחיקת נקודה הופכת אותה לסוג `LinePoint` ב-DB (אם דרך סרביס או טריגר), כדי שכאשר היא חוזרת מנתק דרך נתיב ישן, היא לא תשבור את הרינדור.

### 6. חישוב לגים בנתיב מחדש (Legs Re-calculation)

היות ונתיבים יכולים להשתנות, להדרס או להתמזג עקב ניתוקים:
- **חישוב בזמן אמת:** ה-Leg (קטע הנתיב בין שתי נקודות) מחושב בזמן אמת בלבד (Real-time).
- **שיוך נתונים:** נתונים המבוססים על ה-Leg (למשל התראות) ישויכו תמיד לנקודת היעד (Target WPT) ולאינדקס שלה בתוך ה-`WptList` (ברירת מחדל לאינדקס 0. במידה והנקודה מופיעה יותר מפעם אחת בנתיב, הלוגיקה עוברת למופע הבא שלה). 
- **חישובי גובה ביטחון:** אלו צריכים להימשך מחדש בכל פעם שהנתיב משתנה או חוזר מנתק. שמירה סטטית של גובה הביטחון עלולה להציג ערכים מסוכנים ושגויים על לגים ששינו מיקום (כתוצאה מקונפליקט). כדי לא להעמיס על המעבד, מומלץ להטמיע מנגנון Cache בשרת חישובי השטח לתשובות מהירות עבור פוליגונים שחוזרים על עצמם.