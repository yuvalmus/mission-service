

המערכת מבוססת על ארכיטקטורה המבטיחה שאין אובדן מידע (Zero Data Loss) ושהתקוח (Frontend) תמיד יתסנכרן מחדש למצב העדכני ביותר לאחר נתק או נפילה.

### 1. נפילה או נתק ב-Bridge (שרת התיווך בין ה-DB ל-NATS)

ה-Bridge אחראי להאזין להתראות בסיס הנתונים (`LISTEN entity_changes`) ולהעבירן ל-NATS JetStream.

- **בזמן הנפילה:** בסיס הנתונים (PostgreSQL) ממשיך לקבל עדכונים כרגיל. כל שינוי מעלה את ה-`version` של היישות ומקדם את ה-`mission_change_seq` של המשימה בטבלה.
    
- **התאוששות (Catch-up):**
    
    - בעת עלייתו מחדש, ה-Bridge שואל את NATS JetStream מהו ה-`mission_change_seq` האחרון שנשמר ב-Stream.
        
    - ה-Bridge פונה ל-PostgreSQL ומבצע שאילתת דלתה:
        
        SQL
        
        ````
        SELECT * FROM getEntityDeltaSince(missionId, lastSeqInJetStream)
        ```[cite: 2]
        ````
        
    - כל השינויים שהתקבלו בזמן שה-Bridge היה למטה נשלחים כסדרה ל-JetStream לפי הסדר המדויק, עד להשלמת הפער (Catch-up מלא).
        

### 2. נפילה של NATS / NATS JetStream ומדיניות Connection Strings

שכבת ה-Messaging והאחסון ההמשכי.

- **התמדה לדיסק (Persistence):** NATS JetStream מוגדר לשמור את האירועים על גבי הדיסק (Durable Stream). גם אם שרת ה-NATS קורס או מופעל מחדש, כל ההודעות וה-Sequences שהתקבלו עד לרגע הנפילה נשמרים.
    
- **מדיניות שרשור התחברות (Connection String עם Failover) - Bridge לעומת סנסורים:**
  - **עבור שירות ה-Bridge:** מוגדר באופן קשיח להתחבר **אך ורק ל-NATS המקומי (`localhost:4222`) ללא Failover!**  
    *הסיבה הארכיטקטונית:* אם ה-Bridge יבצע Failover ל-NATS של מחשב אחר ברשת, הוא ישדר התראות על ישויות שעדיין לא השתכפלו לשם דרך `pglogical`. קליינטים במחשב המרוחק יקבלו את ההתראה, יפנו ל-DB המקומי שלהם, ולא ימצאו את המידע (Race Condition). בנוסף, דילוג על ה-NATS המקומי ייצור "חור" ב-Stream המקומי של ה-Leaf Node. במקרה של נפילת NATS מקומי, ה-Bridge נכנס למצב Retry אקספוננציאלי מקומי, ומשלים את האירועים החסרים מה-PostgreSQL המקומי ברגע שה-NATS מתאושש.
  - **עבור שירותי טלמטריה רציפה (חיישנים כגון GPS):** עבורם **מומלץ להשתמש ב-Multi-URL Connection String** (לדוגמה `nats://pc1:4222,nats://pc2:4222`). היות ומידע סנסורי הוא חסר-מצב (Stateless) ואינו תלוי בטרנזקציות DB או ברפליקציה, הפניית הזרם של סנסור ממחשב מקומי תקול אל NATS במחשב אחר מבטיחה שרציפות מיקום ה-GPS לא תיפגע ברחבי הרכב.

### 3. נפילת Frontend / נתק תקשורת ב-WebSocket (WS)

המקרה בו הלקוח (דפדפן React) מאבד חיבור ל-NATS (למשל נפילת רשת, סגירת כרטיסייה, או נפילת שרת ה-Frontend/WS).

- **שמירת מצב מקומי (Dynamic Vector Clock):**
    - ה-Frontend אינו מסתמך על מונה סקאלרי יחיד, אלא מחזיק בזיכרון/IndexedDB שעון וקטורי דינמי (`Record<string, number>`), הממפה כל צומת שיצר מידע (`origin_node`) למונה האחרון שנקלט ממנו (`mission_change_seq`). השעון נבנה דינמית מתוך הישויות הקיימות במערכת.
        
- **חיבור מחדש ושימוש ב-JetStream Catch-up:**
    - כאשר חיבור ה-WebSocket מתאושש, הלקוח מחדש את המנוי מול ה-Stream ומשלים פערים.
        
- **זיהוי דינמי של פערי מידע מ-NATS (Cluster Status Discovery):**
    - כדי לדעת באופן פרואקטיבי האם הוחמצו הודעות מאיזשהו מחשב בזמן הנתק, הלקוח מושך מ-NATS (באמצעות NATS KV קל-משקל `CLUSTER_NODES` או שאילתת `LastValue` על ה-Stream) את הסטטוס העדכני של כל המחשבים ברשת.
    - הלקוח משווה את ה-`vectorClock` שלו מול ה-`last_change_seq` הידוע מכל צומת. במידה ומזוהה פער עבור צומת מסוים, הלקוח פונה מיידית לשאילתת דלתה מול ה-DB.

- **גיבוי (Fallback to DB Delta):**
    - במידה והנתק היה ארוך יתר על המידה וה-Stream ב-NATS אינו מכיל את ההיסטוריה הישנה (בשל מדיניות Retention), ה-Frontend מבצע שאילתת דלתה מול ה-DB תוך העברת ה-`vectorClock` המלא שלו בפורמט JSONB:
      ```sql
      SELECT * FROM entities e
      WHERE e.mission_id = $1 
        AND e.mission_change_seq > COALESCE(($2 ->> e.origin_node)::bigint, 0)
      ORDER BY e.mission_change_seq ASC;
      ```

### 4. ניתוב לשרת בפיגור (Lagging Server & 304 Not Modified)

עבודה מול שרת שלא הספיק להסתנכרן שקולה לחלוטין לארכיטקטורה של **מחשב בנתק**, והמערכת מתמודדת עם זה באופן טבעי:

- **החזרת 304 Not Modified:** כאשר הלקוח שואל "What Changed" ושולח את ה-Vector Clock שלו ב-JSONB (לדוגמה `{"pc1": 10, "pc2": 5}`), השאילתה ב-DB בודקת האם יש ישויות חדשות מכל צומת בהתאמה. אם לשרת שבפיגור אין שום מידע חדש ממה שהלקוח שלח עבור אף צומת, השאילתה תחזיר 0 שורות והשרת יחזיר באלגנטיות `304 Not Modified`. מנגנון זה מונע "שנמוך" (Downgrade) של הנתונים החיים שיש ללקוח בזיכרון.
- **סנכרון מאוחר:** כאשר השרת שבפיגור מסתנכרן לבסוף מהרשת (דרך pglogical), הוא מפיץ התראות. הלקוח ישאל שוב על שינויים עם ה-Vector Clock שלו, והשרת יתעלם מעדכונים שהלקוח כבר מכיר ללא שום בעיה.
- **עריכה מול שרת בפיגור:** במידה והלקוח מבצע עריכות מקומיות מול השרת שבפיגור, נוצר פיצול (Fork) טבעי. המערכת תתייחס לכך בדיוק כמו עריכה בנתק: עם חזרת הסנכרון, הקונפליקט ייפתר אוטומטית באמצעות מנגנוני הגיבוי וזיהוי הכפילויות (מיון דטרמיניסטי לפי UUID).
- **הערה - רענון דפדפן (F5):** בעת רענון מלא של הדפדפן (למשל מול NGINX שניתב לשרת בפיגור), הלקוח מאבד את מצבו הפנימי וטוען את המידע הישן הקיים בשרת. המשתמש יחווה חזרה זמנית בזמן, עד שהשרת יקלוט את העדכונים מ-pglogical וההתראות ירעננו את המפה.
- **ניתוק חלקי (PG מנותק, NATS פעיל):** כל עוד לא התקבלו התראות חדשות ב-NATS וידוע שיש פער, יש לבצע ניסיונות משיכה ב-Interval עם מרווחים אקספוננציאליים (Retry). קבלת התראה חדשה על אותו Origin PC עוצרת את לולאת ה-Retry ומתחילה משיכה מחדש.