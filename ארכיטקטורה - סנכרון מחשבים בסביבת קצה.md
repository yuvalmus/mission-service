# מסמך ארכיטקטורה: סנכרון מחשבים בסביבת קצה

**כותב המסמך:** אסף לומר  
**תאריך:** 1.3.2026  

---

## תוכן עניינים
1. [רקע ומטרות](#1-רקע-ומטרות)
2. [הגדרת דרישות ואילוצים (Requirements)](#2-הגדרת-דרישות-ואילוצים-requirements)
   - 2.1 [אילוצים מבצעיים](#21-אילוצים-מבצעיים)
3. [השוואת חלופות](#3-השוואת-חלופות)
   - 3.1 [רלציוני מול א-רלציוני (Relational vs. Non-Relational)](#31-רלציוני-מול-א-רלציוני-relational-vs-non-relational)
   - 3.2 [Consistency - ההבדל בין Strong ל-Eventual וההשלכות של כל אחד](#32-consistency---ההבדל-בין-strong-ל-eventual-וההשלכות-של-כל-אחד)
   - 3.3 [טכנולוגיות שנבחנו](#33-טכנולוגיות-שנבחנו)
4. [ארכיטקטורת המערכת: Logical Mesh](#4-ארכיטקטורת-המערכת-logical-mesh)
   - 4.1 [רכיבי הפתרון](#41-רכיבי-הפתרון)
   - 4.2 [מימוש טכני והתאמה לחומרה](#42-מימוש-טכני-והתאמה-לחומרה)
     - א. אסטרטגיית מזהים (Identifiers)
     - ב. אופטימיזציה לחומרת 2014 (Memory Tuning)
     - ג. הגדרת pglogical ב-Docker Compose
5. [ניהול מחזור חיים (Operational Flow)](#5-ניהול-מחזור-חיים-operational-flow)
6. [סיכום היתרונות של הארכיטקטורה](#6-סיכום-היתרונות-של-הארכיטקטורה)
7. [הפצת נתוני סנסורים בזמן אמת (Real-Time Sensor Data Distribution)](#7-הפצת-נתוני-סנסורים-בזמן-אמת-real-time-sensor-data-distribution)
   - 7.1 [רקע ומטרות](#71-רקע-ומטרות)
   - 7.2 [השוואת חלופות](#72-השוואת-חלופות)
   - 7.3 [ניהול התראות בטיחות לקליינטים (WebSockets) באמצעות JetStream](#73-ניהול-התראות-בטיחות-לקליינטים-websockets-באמצעות-jetstream)
   - 7.4 [ניהול אמינות התראות (Client-Side ACK over WebSockets)](#74-ניהול-אמינות-התראות-client-side-ack-over-websockets)
   - 7.5 [אסטרטגיית ניהול עומסים ומדיניות Redelivery](#75-אסטרטגיית-ניהול-עומסים-ומדיניות-redelivery)
   - 7.6 [תיעוד וסטנדרטיזציה של ממשקי הודעות (Schema Documentation - AsyncAPI)](#76-תיעוד-וסטנדרטיזציה-של-ממשקי-הודעות-schema-documentation---asyncapi)
8. [סיכום ונקודות פתוחות הנדסית](#8-סיכום-ונקודות-פתוחות-הנדסית)
9. [נספח: ארכיון - חלופת Append-Only Relations](#9-נספח-ארכיון---חלופת-append-only-relations)

---

## 1. רקע ומטרות

המערכת מתוכננת לרוץ בסביבת קצה (רכבים) בשלוש תצורות חומרה אפשריות: מחשב בודד, שני מחשבים או שלושה מחשבים. החומרה מבוססת על מפרטים משנת 2014 (משאבי מעבד וזיכרון מוגבלים).

מטרת העל היא להבטיח שרידות מקסימלית – גם אם מספר מחשבים קורסים או מאבדים מתח, המחשב השורד חייב להמשיך לתפקד, לאסוף נתונים ולכתוב אותם ללא תלות באחרים, תוך שמירה על פשטות פיתוח עבור צוות המפתחים (ג'וניורים ברובם).

---

## 2. הגדרת דרישות ואילוצים (Requirements)

המערכת מיועדת לפעול על 1–3 מחשבי קצה בתוך רכב, המחוברים ברשת LAN פנימית ללא גישה לאינטרנט. החומרה מבוססת על מפרטי 2014 (משאבי CPU ו-RAM מוגבלים).

### 2.1 אילוצים מבצעיים
- **מודל "השורד האחרון" (Survival Mode):** המערכת חייבת לאפשר כתיבה וקריאה מלאה גם אם 2 מתוך 3 מחשבים כשלו פיזית. חל איסור מוחלט על שימוש במנגנוני Quorum (כמו Raft או Paxos) שישתקו את המחשב הנותר (הרחבה בסעיף 3.2).
- **סנכרון P2P:** כל מחשב הוא Peer עצמאי. אין "Master" קבוע.
- **עקביות (Consistency):** נדרשת עקביות בסופו של דבר (Eventual Consistency). במקרה של "פיצול רשת" (Split-Brain), המערכת חייבת להתכנס למצב אחיד אוטומטית.
- **פשטות פיתוח:** שימוש ב-SQL סטנדרטי וטרנזקציות ACID מקומיות. המפתחים לא יכתבו לוגיקת סנכרון בקוד האפליקציה.
- **מחזור חיי נתונים:** הנתונים תקפים לסשן/נסיעה בודדת (עד 12 שעות). בסוף כל סשן מתבצע Reset מלא.

---

## 3. השוואת חלופות

### 3.1 רלציוני מול א-רלציוני (Relational vs. Non-Relational)
ההחלטה הראשונה נוגעת למודל הנתונים שבו תשתמש האפליקציה:

- **א-רלציוני (לדוגמה: Redis / NATS KV):**
  - *יתרונות:* פשוט מאוד למימוש של הגדרות (Settings) או מצב מערכת (Key-Value).
  - *חסרונות:* מקשה מאוד על שמירה ותחקור של נתונים מורכבים (כגון לוגים, היסטוריית חיישנים). ביצוע שאילתות מורכבות, סינון (`WHERE`) או חיבור נתונים (`JOIN`) דורש כתיבת לוגיקה מסובכת ברמת הקוד (אפליקציה). בנוסף ניהול Transactions דורש מימוש עצמאי ולא מובנה בפתרון.
- **רלציוני (SQL):**
  - *יתרונות:* מודל טבעי ומוכר לצוות המפתחים. מאפשר תחקור קל, הבטחת שלמות נתונים (ACID ברמת הטרנזקציה המקומית) וגמישות בשאילתות.
  - *חסרונות:* דורש שינוי של קוד קיים, דורש הטמעת מנגנון סנכרון שכן הוא לא מובנה בפתרון הנבחר (בניגוד ל-NATS KV).
- **ההחלטה:** נבחר מודל רלציוני. כדי לא להעמיס על המפתחים למידת פרדיגמות מורכבות כמו Event Sourcing, עלינו לספק להם ממשק SQL סטנדרטי לעבודה מול בסיס הנתונים.

### 3.2 Consistency - ההבדל בין Strong ל-Eventual וההשלכות של כל אחד
סוגיית העקביות (Consistency) היא נקודת הכשל המרכזית במערכות מבוזרות, במיוחד בתצורות של 2–3 מחשבים:

- **Strong Consistency (עקביות חזקה):**
  - *איך זה עובד:* מבוסס על מנגנוני הסכמה כמו Raft או Paxos (לדוגמה: קלאסטר של Postgres, Redis Sentinel, או LiteFS). דורש הסכמה של "רוב" (Quorum) כדי לאשר כתיבה.
  - *ההשלכה ההרסנית בסביבת קצה:* בתצורה של 3 מחשבים, נדרשים לפחות 2 מחשבים פעילים. אם 2 קורסים, המחשב השלישי ננעל והופך ל-Read-Only. בתצורה של 2 מחשבים, קריסה של מחשב אחד עוצרת את המערכת לחלוטין (כי 1 אינו רוב של 2).
  - *מסקנה:* עקביות חזקה מהווה סכנה קיומית למערכת שלנו. היא תגרום להשבתת איסוף הנתונים דווקא כשהתנאים קשים.
- **Eventual Consistency (עקביות בסופו של דבר):**
  - *איך זה עובד:* כל צומת (מחשב) כותב את הנתונים באופן מקומי ומיידי, ומשדר אותם לאחרים ברקע כשהרשת זמינה.
  - *ההשלכה בסביבת קצה:* שרידות מוחלטת ("Shared Nothing"). אם 2 מתוך 3 מחשבים נשרפים, המחשב הנותר כלל לא מודע לכך מבחינת יכולת הכתיבה שלו – הוא פשוט ממשיך לשמור נתונים מקומית. כשהמחשבים האחרים יחזרו, הם "ידביקו את הפער" (Catch-up).
  - *מסקנה:* זהו המודל היחיד שמאפשר עבודה רציפה ובלתי תלויה, החיונית לסביבת הקצה.

### 3.3 טכנולוגיות שנבחנו
בחנו מספר גישות וטכנולוגיות כדי לממש בסיס נתונים רלציוני עם Eventual Consistency:

1. **Postgres/MySQL כקלאסטר מבוסס Quorum:**  
   *פסילה:* צורך משאבי עיבוד וזיכרון גבוהים מדי לחומרת 2014. מבוסס על עקביות חזקה (יקרוס ללא Quorum), דורש הקמה של מספר שירותים על אותו המחשב בטכנולוגיות שונות על מנת לנהל את ה-Cluster.
2. **מנועי סנכרון כבדים (SymmetricDS):**  
   *פסילה:* למרות שהוא מספק Eventual Consistency מצוין עבור SQL, מדובר בכלי מבוסס Java שצורך הרבה זיכרון RAM ודורש ניהול תצורה (XML/Triggers) שעלול להיות מסובך לתחזוקה עבור צוות הפיתוח.
3. **RabbitMQ:**  
   *פסילה:* מנגנוני השרידות המודרניים שלו (Quorum Queues) מבוססים על פרוטוקול Raft. כפי שהוסבר בסעיף 3.2, מודל זה ינעל את המחשב הנותר במקרה של נפילת "רוב", ובכך יכשיל את דרישת "השורד האחרון".
4. **Distributed SQLite (LiteFS / dqlite / rqlite):**  
   *פסילה:* למרות ש-SQLite חסכוני במשאבים, הכלים הללו עטופים במנגנון Raft. המשמעות היא שהם יסבלו מבעיית ה-Quorum וינעלו את המערכת במקרה של קריסת רוב המחשבים.
5. **Redis Sentinel / Streams:**  
   *פסילה:* צורך זיכרון רב (In-Memory), מסבך את הפיתוח (ניהול Consumer Groups), ושוב – קורס ללא Quorum.

---

## 4. ארכיטקטורת המערכת: Logical Mesh

המערכת תוקם בתצורת **Full Mesh**: כל מחשב מריץ מופע PostgreSQL מקומי ומחובר לשני המחשבים האחרים ברפליקציה לוגית דו-כיוונית.

```
+-------------------------------------------------------------+
|                        Vehicle LAN                          |
|                                                             |
|   +-------------------+             +-------------------+   |
|   |      PC 1         |             |      PC 2         |   |
|   | [App] <-> [DB]    | <===P2P===> | [App] <-> [DB]    |   |
|   +-------------------+             +-------------------+   |
|             ^                                 ^             |
|              \                               /              |
|               \==== P2P Logical Sync =======/               |
|                               v                             |
|                     +-------------------+                   |
|                     |      PC 3         |                   |
|                     | [App] <-> [DB]    |                   |
|                     +-------------------+                   |
+-------------------------------------------------------------+
```

### 4.1 רכיבי הפתרון
1. **מנוע בסיס הנתונים:** PostgreSQL 16 בתצורה "רזה" (Tuned for 2014 hardware).
2. **מנוע הסנכרון:** הרחבת `pglogical`. בניגוד לרפליקציה הסטנדרטית של Postgres, הרחבה זו מאפשרת ניהול קונפליקטים אוטומטי.
3. **מדיניות יישוב קונפליקטים:** הגדרת **Last Update Wins (LWW)** מבוסס חותמת זמן (`last_update_time`). אם שני מחשבים עדכנו את אותה שורה בזמן נתק, המערכת תבחר אוטומטית בגרסה החדשה ביותר לפי חותמת זמן ברגע החיבור מחדש. בסביבת Standalone שבה סשן עבודה הוא עד 12 שעות והפעולות מונעות ע"י משתמש, סטיות זמנים קלות מתקבלות.

### 4.2 מימוש טכני והתאמה לחומרה

#### א. אסטרטגיית מזהים (Identifiers)
כדי למנוע התנגשויות (Primary Key Conflicts) בזמן עבודה מנותקת, **חל איסור על שימוש ב-Serial/BigInt כמפתח ראשי**.
- כל הטבלאות ישתמשו ב-`UUIDv4` כמפתח ראשי.
- זה מבטיח שכל שורה שנוצרת במחשב א' לעולם לא תתנגש עם שורה שנוצרת במחשב ב'.

#### ב. אופטימיזציה לחומרת 2014 (Memory Tuning)
כדי למנוע מ-Postgres לצרוך את כל משאבי המערכת המוגבלים, נגדיר ב-`postgresql.conf`:
```ini
max_connections = 20           # צמצום Overhead של תהליכים
shared_buffers = 128MB         # חיסכון ב-RAM
wal_level = logical            # הכרחי עבור pglogical
max_worker_processes = 10      # הגבלה של תהליכי הרפליקציה
max_replication_slots = 10
max_wal_senders = 10
```

#### ג. הגדרת pglogical ב-Docker Compose
כל מחשב יריץ את ה-Image הכולל את ההרחבה. האפליקציה תתחבר תמיד ל-`localhost:5432`:
```yaml
services:
  db:
    image: point6/postgres-pglogical:16
    environment:
      - POSTGRES_DB=vehicle_data
    command:
      - postgres
      - -c
      - "shared_preload_libraries=pglogical"
      - -c
      - "max_replication_slots=10"
      - -c
      - "max_wal_senders=10"
```

---

## 5. ניהול מחזור חיים (Operational Flow)

1. **עבודה שוטפת:** האפליקציה כותבת ל-Postgres המקומי. הסנכרון לשאר המחשבים מתבצע ברקע (Asynchronous).
2. **נתק רשת / קריסת מחשבים:** המחשבים הנותרים ממשיכים לעבוד כרגיל. אין "Failover" מורכב כי כל אפליקציה עובדת מול ה-DB המקומי שלה.
3. **התאוששות (Self-Healing):** ברגע שמחשב שקרס חוזר לפעולה, ה-`pglogical worker` מזהה את ה-Peers שלו ומשלים את הפערים (Catch-up) מהנקודה שבה הפסיק.
4. **איפוס סשן (Factory Reset):** מתבצע באמצעות פקודת `TRUNCATE` על אחד המחשבים. הפקודה משתכפלת מיידית לכל רשת ה-Mesh ומנקה את כל בסיסי הנתונים בו-זמנית.

---

## 6. סיכום היתרונות של הארכיטקטורה
- **שרידות אבסולוטית:** המערכת עומדת בדרישת "השורד האחרון" ללא צורך ב-Quorum.
- **ביצועים:** אפס שיהוי רשת (Network Latency) בכתיבה, כיוון שהיא מתבצעת מקומית.
- **פשטות לצוות הפיתוח:** מודל SQL רלציוני מלא. אין צורך בניהול "מצבי רשת" בתוך הקוד.
- **התאמה לחומרה:** שימוש יעיל במשאבים הודות ל-Tuned Postgres ו-UUIDs.

---

## 7. הפצת נתוני סנסורים בזמן אמת (Real-Time Sensor Data Distribution)

### 7.1 רקע ומטרות
המערכת ברכב קולטת נתונים מסנסורים בתדירות גבוהה (כגון GPS), אשר צריכים להיות מופצים בזמן אמת למספר שירותים על גבי המחשבים השונים (למשל: שירות אחד מבצע חישובים על הנתונים, ושירות אחר שומר אותם או מציג אותם במסך).  
המטרה היא להעביר את הנתונים ביעילות המקסימלית, ללא יצירת עומס על חומרת 2014 המוגבלת, ותוך חיסכון בכתיבת קוד תשתיתי מסובך על ידי צוות המפתחים.

### 7.2 השוואת חלופות
בחנו מספר דרכים להפצת הנתונים ברשת המקומית (LAN Switch) בין המחשבים:
- **Broadcast מבוסס רשת (L2 UDP):**  
  *פסילה:* למרות שזה פשוט ברמת הרשת, זה מעביר את כל נטל הסינון לאפליקציה. כל מחשב מקבל כל פאקט, והמעבד החלש נאלץ "להתעורר" כדי לבדוק אם המידע רלוונטי לו או לזרוק אותו. בנוסף, חסרה אמינות ברמת הפרוטוקול (הודעות הולכות לאיבוד ללא התראה) ואין תמיכה בניתוב חכם.
- **חיבורים ישירים (Raw TCP/UDP Sockets):**  
  *פסילה:* דורש מצוות הפיתוח לכתוב ולתחזק קוד תשתיתי מורכב: ניהול חיבורים, זיהוי ניתוקים, ניסיונות חיבור חוזרים (Retries), וניהול עומסים. זה ייצור ארכיטקטורת "ספגטי" שתקשה על הוספת שירותים חדשים בעתיד.
- **משיכת נתונים (Polling ממסד נתונים או API):**  
  *פסילה:* שירותים ששואלים "האם יש נתון חדש?" בלולאה גוזלים משאבי CPU ורשת מיותרים באופן דרסטי. גישה זו גם מייצרת שיהוי (Latency) שאינו מקובל בעבודה מול סנסורים בזמן אמת.

**הפתרון הנבחר: NATS Pub/Sub עם תמיכה ב-Queue Groups**  
בהמשך לבחירה ב-NATS כתשתית התקשורת (Leaf Nodes), נשתמש ביכולות הליבה שלו (NATS Core) כדי לנהל את תעבורת הסנסורים:
1. **ניתוב חכם ויעילות חומרה (Subject-Based Routing):** הנתונים נשלחים לערוץ ספציפי (למשל `sensor.gps`). רק מחשבים ושירותים שבאמת צריכים את המידע מאזינים לו. זה חוסך מחזורי מעבד יקרים בחומרה הישנה שלנו.
2. **מניעת כפילות חישוב (Queue Groups):** כדי למנוע מצב שבו 3 המחשבים מחשבים את אותו נתון GPS במקביל ומבזבזים פי 3 CPU ממה שנדרש, נשתמש ב-Queue Groups. המפתחים פשוט מוסיפים שם קבוצה (למשל `gps-calc`) בעת ההרשמה (`Subscribe`). NATS ידאג לנתב את הפאקט רק למחשב אחד הפנוי באותו רגע. אם הוא קורס, NATS יעביר את הנתונים מיד למחשב השורד (Failover מיידי).
3. **Fan-out מובנה (שימוש מקביל בנתונים):** אותו נתון GPS שנשלח פעם אחת, יכול להגיע למחשב אחד עבור חישוב (דרך Queue Group), ובמקביל להגיע לכל המחשבים האחרים שצריכים אותו רק לתצוגה או רישום (דרך Pub/Sub רגיל).
4. **חוויית פיתוח חלקה (DX):** המפתחים לא מתעסקים ברשת. הם כותבים שורת קוד אחת של `publish` או `subscribe`. מנגנוני ה-Reconnects, ניהול התורים ואיזון העומסים (Load Balancing) מטופלים כולם על ידי השרת של NATS שרץ ברקע.
5. **מדיניות שרשור התחברות (Multi-URL Connection String):**
   - **עבור שירותי סנסורים (כגון GPS):** מוגדר לעבוד עם שרשור Multi-URL מלא (`nats://pc1:4222,nats://pc2:4222,nats://pc3:4222`). אם שרת ה-NATS המקומי של המחשב שאליו מחובר הסנסור כושל, שירות ה-GPS מדלג אוטומטית ל-NATS בצומת אחר וממשיך להפיץ את המיקום הרציף לכל שאר הרכב.
   - **עבור שירות ה-Bridge (אירועי DB):** מוגדר להתחבר **אך ורק ל-`localhost:4222`**. חל איסור על Failover ב-Bridge למחשב אחר, כדי למנוע מצב של Race Condition שבו מופצת התראה ב-NATS מרוחק לפני שרפליקציית ה-DB (`pglogical`) השלימה את הסנכרון, וכדי למנוע שבירת ה-Stream המקומי של ה-Leaf Node. במקרה של כשל ב-NATS מקומי, ה-Bridge ממתין ב-Retry ומשלים את האירועים מה-PG המקומי עם התאוששות ה-Broker.

```
                      [ GPS Sensor ]
                            │
                       Serial / UDP
                            │
                            ▼
                    ┌───────────────┐
                    │ PC 1 (Worker) │
                    │ NATS Publish  │
                    │ NATS LeafNode │
                    └───────┬───────┘
           ┌────────────────┼────────────────┐
           ▼                                 ▼
   ┌───────────────┐                 ┌───────────────┐
   │ PC 2 (Worker) │                 │ PC 3 (Worker) │
   │ NATS LeafNode │                 │ NATS LeafNode │
   └───────┬───────┘                 └───────┬───────┘
           │                                 │
     Queue Group:                      Queue Group:
       gps-calc                          gps-calc
    [ Calc Service ]                  [ Calc Service ]
           │                                 │
    [ Monitor Service ]               [ Monitor Service ]
```

### 7.3 ניהול התראות בטיחות לקליינטים (WebSockets) באמצעות JetStream
עבור התראות בטיחות המיועדות לקליינטים (UIs) המתחברים ב-WebSockets, נשתמש ב-JetStream Push Consumers. בחירה זו אינה רק עניין של הפצה, אלא הבטחת שרידות המידע (Reliability) בסביבה תנודתית:
- **שימוש ב-NATS Leaf Nodes למניעת תלות ב-Quorum:** המימוש יתבצע על גבי NATS Leaf Nodes. בעוד ש-JetStream בתצורה סטנדרטית מבוסס לרוב על פרוטוקול Raft (הדורש Quorum של רוב צמתים כדי לתפקד), עבודה במודל Leaf Nodes מאפשרת לכל מחשב לנהל Stream מקומי ועצמאי לחלוטין. בדרך זו, אנו נהנים מיכולות ההתמדה והשידור החוזר של JetStream מבלי להסתכן בשיתוק המערכת במקרה של נפילת שני מחשבים, כפי שקורה בקלאסטר Raft מסורתי.
- **הבטחת הגעה חוצת-מחשבים (Stream Mirroring/Sourcing):** כדי לוודא שהתראה שנוצרה במחשב א' תגיע בוודאות ל-UI המחובר למחשב ג', נגדיר את ה-Streams במודל של Mirror/Source. כל Leaf Node יוגדר "למשוך" (Pull) נתונים מה-Streams של עמיתיו. ברגע שהקשר בין המחשבים חוזר, ה-Leaf Nodes מבצעים סנכרון אוטומטי של הודעות חסרות מהדיסק המקומי של המחשב היוצר אל הדיסק של שאר המחשבים. מנגנון זה מבטיח שהודעה לא "תתאדה" בזמן נתק, אלא תמתין ב-Log עד להשלמת הסנכרון והעברתה לכל הצרכנים הרלוונטיים.
- **יכולת "השלמת פערים" (Catch-up):** חיבורי WebSockets נוטים להתנתק עקב הפרעות קליטה או עומס רגעי. ללא JetStream, התראה שנשלחה בזמן שהקליינט היה ב-Reconnect "תתאדה". באמצעות Stream, ברגע שהקליינט מתחבר מחדש, הוא יכול לבקש לקבל את כל ההודעות שפספס (לפי Sequence או זמן), ובכך להבטיח ששום התראת בטיחות לא נעלמה מהמסך של המשתמש.
- **מניעת הצפה (Flow Control):** דפדפנים וקליאנטים חלשים עלולים להיחנק מהצפת הודעות. JetStream מאפשר להגדיר Rate Limit ברמת ה-Consumer, כך ש-NATS "ירסן" את קצב שליחת ההתראות לקליינט וימנע קריסה של ממשק המשתמש על חומרת הקצה.
- **מבט ביקורתי על WebSockets:** שימוש ב-WS להעברת מידע בטיחותי (Safety-Critical) הוא נקודת תורפה. ה-Overhead של TCP והתלות ביציבות ה-Browser הם סיכון. לכן, ה-Stream ב-NATS משמש כ"קופסה שחורה" ששומרת את ההיסטוריה (Persistent Log), כך שגם אם ה-UI קרס, המידע עדיין קיים ב-Broker וניתן לתחקור (Audit) לאחר אירוע.

### 7.4 ניהול אמינות התראות (Client-Side ACK over WebSockets)
כדי להבטיח שהתראת בטיחות לא רק "נשלחה" אלא גם "התקבלה" והוצגה למשתמש, נממש מנגנון אישור אקטיבי המבוסס על יכולות ה-JetStream:
- **סגירת מעגל (End-to-End ACK):** הקליינט (הדפדפן/UI) לא רק מקבל את ההודעה, אלא מחזיר פקודת ACK ל-NATS. רק לאחר קבלת האישור מהקליינט, ה-JetStream יסמן את ההודעה כ"נמסרה". חשוב להנחות את הצוות לא לממש "ACK אפליקטיבי" (כלומר, לשלוח הודעת Pub/Sub חוזרת שהם כתבו), אלא להשתמש ב-Native ACK של פרוטוקול NATS. זה יחסוך כתיבת לוגיקת Retries מורכבת בקוד האפליקציה וישאיר את הניהול הכבד ל-NATS Engine, שכתוב ב-Go ויודע להתמודד עם חומרת 2014 הרבה יותר טוב מאשר קוד JS.
- **טיפול בניתוקים (Redelivery Policy):** במידה והקליינט התנתק או קפא לפני ששלח ACK, ה-JetStream ינסה לשלוח את ההתראה שוב (Retry) לקליינט ברגע שיתחבר מחדש, או לקליינט אחר המחובר לאותה קבוצה (Queue Group). זהו "רשת ביטחון" קריטית שמונעת מצב שבו התראה קריטית "נעלמת" בגלל באג ב-JS או נפילת רשת מקומית ברכב.
- **ניהול עומס (Max Ack Pending):** כדי למנוע מצב שבו קליינט איטי תוקע את כל התור, נגדיר פרמטר `MaxAckPending`. אם הקליינט לא אישר מספר מסוים של התראות, המערכת תדע לעצור את ניסיונות השליחה, מה שמונע "זליגת זיכרון" בשרת ה-NATS המוגבל במשאבים.

### 7.5 אסטרטגיית ניהול עומסים ומדיניות Redelivery
כדי להבטיח שהמערכת לא תבצע "התאבדות" (Self-DOS) בזמן עומס או ניתוקים, נגדיר מדיניות נוקשה ל-Consumer של התראות הבטיחות:

1. **פרמטר MaxAckPending (חסימת זליגת זיכרון):**  
   במחשבים ישנים, זיכרון ה-RAM הוא המשאב הראשון שנגמר.
   - *המנגנון:* נגדיר `MaxAckPending: 100` (לדוגמה). אם ישנן 100 התראות שנשלחו לקליינט אך טרם אושרו, NATS יפסיק מיידית לשלוח הודעות חדשות לאותו קליינט.
   - *התועלת:* זה מונע מ-NATS לצבור "Queue" אינסופי בזיכרון עבור קליינט איטי או תקוע. ברגע שהקליינט מתאושש ומאשר הודעות, הזרם מתחדש אוטומטית.
2. **מדיניות שליחה חוזרת (Redelivery Policy):**  
   כדי לא ליצור "סערת מעבד" (CPU Storm) בזמן תקלת תקשורת, נגדיר:
   - `AckWait`: הזמן ש-NATS מחכה ל-ACK לפני שהוא מחשיב את ההודעה ככשל (נמליץ על 5–10 שניות).
   - `MaxDeliver`: הגבלת מספר הניסיונות (למשל 3–5). אם הודעה לא אושרה אחרי X ניסיונות, היא תועבר ל-DLQ (Dead Letter Queue).
   - *למה זה חשוב?* הודעה שגורמת לקריסה בקליינט (Poison Message) עלולה להיכנס ללולאת שליחה אינסופית שתפיל את כל הקליינטים בזה אחר זה. הגבלת הניסיונות עוצרת את כדור השלג הזה.
3. **מרווחי זמן (Backoff):**  
   במקום לנסות לשלוח שוב מיד, נשתמש ב-Backoff. לדוגמה: ניסיון ראשון אחרי 5 שניות, שני אחרי 30 שניות, שלישי אחרי דקה. זה נותן למערכת "זמן נשימה" להתאושש מתקלות רשת רגעיות מבלי להציף את ה-WebSocket.
4. **אופטימיזציה לעדכניות (Last-Value Cache & Ring Buffer):**  
   כדי למנוע מה-UI "לרדוף" אחרי מידע היסטורי לא רלוונטי (Lag) לאחר ניתוק, ולשמירה על יציבות הזיכרון בחומרה המוגבלת:
   - *מדיניות Discard Old:* ה-Stream יוגדר כ-Ring Buffer עם הגבלת כמות (למשל `MaxMsgs: 100`). ברגע הגעה לסף, הודעות חדשות ידחקו החוצה את הישנות ביותר אוטומטית. זה מבטיח שה-Broker לעולם לא יחרוג מהקצאת הזיכרון המוגדרת.
   - *מדיניות Deliver Policy (Last/New):* ה-Consumers ב-UI יוגדרו לקבלת הודעות מסוג `DeliverLast` (הנתון האחרון בלבד כ-Snapshot) או `DeliverNew` (הודעות חדשות בלבד).
   - *הפרדת רשויות (Persistence vs. Real-time):* נתונים היסטוריים לתחקור נשמרים ב-Postgres בלבד. ה-NATS משמש אך ורק להפצה אקטואלית. גישה זו מבטיחה שה-UI יציג תמיד את המצב הנוכחי של הרכב בזמן אמת, ללא שיהוי הצטברותי (Accumulated Latency).

**דוגמת הגדרות ב-JSON Schema עבור Consumer:**
```json
{
  "stream_name": "SENSOR_DATA",
  "config": {
    "deliver_policy": "last",
    "max_ack_pending": 100,
    "ack_policy": "explicit",
    "replay_policy": "instant"
  }
}
```

### 7.6 תיעוד וסטנדרטיזציה של ממשקי הודעות (Schema Documentation - AsyncAPI)
כיוון ש-NATS הוא מטבעו Schema-less (הוא מעביר Payload של Bytes מבלי לאכוף מבנה), קיים סיכון ששינוי במבנה הודעה במחשב א' יפיל את השירות במחשב ב'. כדי למנוע זאת, נאמץ את תקן **AsyncAPI**:
1. **ה"Swagger" של עולם האירועים:** בדומה לשימוש ב-OpenAPI בעולם ה-REST, נשתמש ב-AsyncAPI כדי להגדיר בצורה פורמלית את:
   - Channels: לדוגמה `sensor.gps` או `safety.alert`.
   - Payloads: מבנה הנתונים (JSON Schema) כולל שדות חובה וטיפוסי נתונים.
   - Bindings: הגדרות ספציפיות ל-NATS (כמו Queue Groups).
2. **יתרונות לצוות הפיתוח:**
   - *Single Source of Truth:* מסמך JSON/YAML אחד שמתאר את כל האינטראקציות בין המחשבים ברכב.
   - *יצירת קוד אוטומטית (Code Gen):* מפתחים יוכלו לייצר Typescript/Go Types ישירות מהגדרת ה-AsyncAPI, מה שימנע שגיאות כתיב בשמות שדות (Typo) או בנתיבי Subjects.
   - *תיעוד ויזואלי:* שימוש ב-AsyncAPI Studio מאפשר למפתחים לראות את ה-Flow של המערכת בצורה גרפית, מה שמקל על הבנת הארכיטקטורה המבוזרת.
3. **אכיפת חוזים (Contract Testing):** בשלב מתקדם, נשלב ב-CI/CD בדיקה שמוודאת שכל שינוי בקוד האפליקציה תואם את ה-Schema שהוגדרה ב-AsyncAPI. זהו קריטי במערכת מבוזרת שבה קשה מאוד לבצע Rollback לכל המחשבים בו-זמנית בזמן תקלה.

**דוגמה לתמצית הגדרת Subject ב-AsyncAPI:**
```yaml
channels:
  sensor.gps:
    publish:
      message:
        payload:
          type: object
          properties:
            lat: { type: number }
            lon: { type: number }
            alt: { type: number }
          required: [lat, lon]
```

---

## 8. סיכום ונקודות פתוחות הנדסית

הארכיטקטורה המוצעת זונחת את מודל ה-Quorum המסורתי, שאינו מתאים לסביבת קצה תנודתית, לטובת מודל **Shared Nothing** המותאם לחומרה ישנה. על ידי שילוב בין PostgreSQL בתצורת Mesh, הרחבת `pglogical` ותשתית מסרים מבוססת NATS JetStream, יצרנו מערכת שמתעצמת דווקא בנקודות הכשל שלה: היא מעדיפה המשכיות מבצעית על פני עקביות תיאורטית ומבטיחה ש"השורד האחרון" תמיד יתפקד באופן מלא ומנותק. גישה זו מספקת מענה מעשי למגבלות החומרה והשרידות, תוך שהיא מאפשרת לצוות הפיתוח להתמקד בלוגיקה האפליקטיבית מבלי לנהל את מורכבות הסנכרון והרשת.

### נקודות פתוחות הנדסית וסטטוס מעודכן:
1. **קונפליקטים של שעונים:**  
   "Last Update Wins" מסתמך על שעון המחשב. בסביבת קצה ללא NTP חיצוני, סטייה של כמה מילי-שניות בין מחשבים היא ודאית.  
   *הכרעה ארכיטקטונית עדכנית:* סשן שימוש במערכת הוא עד 12 שעות בסביבת Standalone והפעולות הן פעולות משתמש, ולכן לא סביר שתהיה סטייה משמעותית שתפגע בעקביות, וההסתמכות על `last_update_time` מתקבלת.
2. **פשטות למפתחי ג'וניור:**  
   אמנם SQL מוכר להם, אך ניהול קונפליקטים ב-pglogical וניפוי שגיאות (Debugging) ב-Mesh דו-כיווני הוא אירוע מורכב שדורש הבנה עמוקה של Replication Slots. הטיפול מבוצע ע"י מעטפת טריגרים ואוטומציה ברמת ה-DB כדי לבודד את המפתחים מהמורכבות.
3. **מעבר מ-Docker ל-Native:**  
   בחומרה כזו, כל קילו-בייט נחשב. נשקלת האפשרות להריץ את ה-Binaries ישירות על ה-OS (אם ניתן) כדי לחסוך ב-Overhead של ה-Container Runtime.
4. **Hybrid Storage:**  
   נשקול להשתמש ב-NATS KV להגדרות דינמיות (Settings) ורק ב-Postgres לנתונים רלציוניים. אין טעם לסנכרן טבלאות SQL עבור דגלים פשוטים (Booleans).
5. **שעון לוגי ושעון וקטורי דינמי (Dynamic Vector Clock):**  
   במקום להסתמך רק על TIMESTAMPTZ או על מונה סקאלרי יחיד (שעלול להתנגש כאשר מחשבים מתקדמים במקביל בנתק), המערכת משלבת את מזהה הצומת (`origin_node`) יחד עם מונה המשימה האטומי (`missions.last_change_seq`) ליצירת מזהה גרסה ייחודי (למשל `PC1-1`, `PC2-3`). הלקוח מחזיק שעון וקטורי דינמי (`Record<string, number>`) הנבנה אוטומטית מתוך הישויות ללא הגבלה למספר הצמתים, ומבצע שאילתות דלתה מבוססות JSONB ב-PostgreSQL.

---

## 9. נספח: ארכיון - חלופת Append-Only Relations

> **הערה היסטורית:** חלופה זו נבחנה במקור ונפסלה/הועברה לארכיון. נכון להיום **לא נשתמש בה**, והמערכת עובדת עם שיוך רלציוני ישיר בטבלת הישויות (`mission_id`, `infra_id`, `parent_entity_id`). התיעוד נשמר לצורכי תחקור ורפרנס הנדסי בלבד.

כדי לנהל קשרי "רבים-לרבים" (נקודות, פוליגונים וסלים) ללא חשש מאיבוד נתונים או "חצאי טרנזקציות", נבחן מודל של הוספה בלבד (Insert-Only) עבור קשרים:

### א. מבנה טבלת הקשרים (Junction Table)
במקום לעדכן או למחוק שורות, כל שינוי בקשר בין נקודה לפוליגון נרשם כאירוע חדש:
```sql
CREATE TABLE point_polygon_relations (
    id UUID PRIMARY KEY,                   -- מזהה ייחודי של הקשר
    point_id UUID NOT NULL,               -- מזהה הנקודה
    polygon_id UUID NOT NULL,             -- מזהה הפוליגון
    relation_status TEXT,                 -- חיבור ('connected') או ניתוק ('disconnected')
    created_at TIMESTAMPTZ DEFAULT now()
);
```

### ב. לוגיקת העבודה (למפתחים):
- **חיבור:** בצע `INSERT` עם סטטוס `connected`.
- **ניתוק:** אל תבצע `DELETE`. בצע `INSERT` חדש עם סטטוס `disconnected`.
- **יתרון:** מונע "חזרת נתונים מהמתים" (Zombie Data) ומבטיח שהיסטוריית הקשרים תסתנכרן בצורה מושלמת גם לאחר נתק ממושך.

### ג. שליפת המצב הנוכחי (View)
כדי להקל על המפתחים, יצירת View שמציג רק את הקשרים הפעילים כרגע (הסטטוס האחרון של כל צמד הוא `connected`):
```sql
CREATE VIEW active_relations AS
SELECT point_id, polygon_id
FROM (
    SELECT DISTINCT ON (point_id, polygon_id) 
        point_id, 
        polygon_id,
        relation_status
    FROM point_polygon_relations
    ORDER BY point_id, polygon_id, created_at DESC
) as latest
WHERE relation_status = 'connected';
```
