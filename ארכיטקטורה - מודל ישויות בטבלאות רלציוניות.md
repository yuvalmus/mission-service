# מסמך ארכיטקטורה: מודל ישויות בטבלאות רלציוניות

**כותב המסמך:** אסף לומר  
**תאריך:** 5.3.2026  

---

## תוכן עניינים
1. [רקע ומטרות](#1-רקע-ומטרות)
2. [הגדרת דרישות ואילוצים (Requirements)](#2-הגדרת-דרישות-ואילוצים-requirements)
3. [השוואת חלופות מרחביות](#3-השוואת-חלופות-מרחביות)
   - 3.1 [בחירת מודל אחסון](#31-בחירת-מודל-אחסון)
   - 3.2 [רזולוציית קואורדינטות (Precision)](#32-רזולוציית-קואורדינטות-precision)
4. [ארכיטקטורת הנתונים המרחבית (Spatial Data Model)](#4-ארכיטקטורת-הנתונים-המרחבית-spatial-data-model)
   - 4.1 [סכימת ליבה ושיוך בלעדי (Exclusive Belongs-To)](#41-סכימת-ליבה-ושיוך-בלעדי-exclusive-belongs-to)
   - 4.2 [אכיפת סמנטיקה כקו הגנה למערכת מבוזרת](#42-אכיפת-סמנטיקה-כקו-הגנה-למערכת-מבוזרת)
   - 4.3 [צינור נתונים ל-WebGL (High-Performance WebGL Pipeline)](#43-צינור-נתונים-ל-webgl-high-performance-webgl-pipeline)
   - 4.4 [מדיניות מחיקה היררכית (Top-Down View Masking)](#44-מדיניות-מחיקה-היררכית-top-down-view-masking)
5. [ניהול מחזור חיים והפצת נתונים (Operational Flow)](#5-ניהול-מחזור-חיים-והפצת-נתונים-operational-flow)
   - 5.1 [הפצה בתחומי אחריות שונים](#51-הפצה-בתחומי-אחריות-שונים)
     - 5.1.1 [מניעת כפילויות בהפצה (Distributed Deduplication)](#511-מניעת-כפילויות-בהפצה-distributed-deduplication)
     - 5.1.2 [הבטחת מסירה ומניעת אובדן הודעות (Reliable Bridge)](#512-הבטחת-מסירה-ומניעת-אובדן-הודעות-reliable-bridge)
   - 5.2 [ניהול מחזור חיים: מחיקה היררכית היברידית (Hybrid Deletion)](#52-ניהול-מחזור-חיים-מחיקה-היררכית-היברידית-hybrid-deletion)
   - 5.3 [אופרציות כבדות: שכפול עמוק בתוך המאגר - Atomic Cloning](#53-אופרציות-כבדות-שכפול-עמוק-בתוך-המאגר---atomic-cloning)
6. [ניהול גרסאות נתונים ותאימות (Schema & Data Versioning)](#6-ניהול-גרסאות-נתונים-ותאימות-schema--data-versioning)
7. [התאמה לתצורת ענן וריבוי משתמשים (Cloud & Backend Scaling)](#7-התאמה-לתצורת-ענן-וריבוי-משתמשים-cloud--backend-scaling)
   - 7.1 [מעבר מ-Push מבוסס Trigger ל-CDC](#71-מעבר-מ-push-מבוסס-trigger-ל-cdc)
   - 7.2 [אופטימיזציית שאילתות בנפחי ענק](#72-אופטימיזציית-שאילתות-בנפחי-ענק)
   - 7.3 [ניהול קונפליקטים ו-Connection Pooling](#73-ניהול-קונפליקטים-ו-connection-pooling)
8. [סיכום היתרונות של הארכיטקטורה המרחבית](#8-סיכום-היתרונות-של-הארכיטקטורה-המרחבית)
9. [נקודות פתוחות הנדסית](#9-נקודות-פתוחות-הנדסית)

---

## 1. רקע ומטרות

כחלק ממערכת השליטה והבקרה הרצה על רכבים בסביבת קצה, נדרש ניהול של מידע מרחבי מורכב: משימות (Missions), תשתיות קבועות (Infra), וישויות טקטיות (Entities כגון פוליגונים, מסלולים, ונקודות עניין). מטרת העל היא להטמיע את המנוע הגיאוגרפי (PostGIS) לתוך הארכיטקטורה המבוזרת (Logical Mesh) שהוגדרה, תוך התחשבות במשאבי המעבד (CPU) והזיכרון (RAM) המוגבלים של מפרטי 2014, והבטחת שרידות נתונים בתרחישי נתק וקריסת מערכות מרובות ("השורד האחרון").

מסמך זה מהווה הרחבה למסמך `ארכיטקטורה - סנכרון מחשבים בסביבת קצה`.

---

## 2. הגדרת דרישות ואילוצים (Requirements)

בהמשך לאילוצים המבצעיים שהוגדרו (Eventual Consistency, פשטות פיתוח לצוות):
- **יעילות חישובית:** מנועי GIS נוטים להיות צרכני CPU כבדים. יש למנוע ממחשבי 2014 לבצע מתמטיקה כבדה שאינה הכרחית.
- **עקביות מרחבית בנתק:** במצב של Split-Brain, אם מחשב א' מעדכן פוליגון ומחשב ב' מוסיף לו נקודה, עם החזרה לשגרה (Catch-up) הנתונים חייבים להשתלב מבלי לייצר "גיאומטריות שבורות".
- **הגנה על ה-WebGL (UI):** מכיוון שכוח העיבוד בקצה חלש, יש למנוע מהדפדפן לבצע המרות (Parsing) ל-GeoJSON כבדים. ה-DB יידרש להכין את הנתונים כ-Payload "לעוס" ומוכן לרינדור.

---

## 3. השוואת חלופות מרחביות

### 3.1 בחירת מודל אחסון
בניגוד למערכות מפות סטנדרטיות המבצעות הטלה (Projection) ל-Web Mercator, המערכת שלנו משתמשת ב-OpenLayers WebGL בסטנדרט **EPSG:4326**.

**ההחלטה:** עמודות המידע יישמרו כ:
```sql
GEOMETRY(Geometry, 4326)
```

### 3.2 רזולוציית קואורדינטות (Precision)
- **תצורה סטנדרטית (15 ספרות עשרוניות):** מייצרת GeoJSON עצום בגודלו, חונקת את רשת ה-LAN המקומית, וגורמת לפקק ב-`JSON.parse` בדפדפן.
- **ההחלטה:** יישום Quantization בשרת. שימוש בפונקציה:
  ```sql
  ST_SnapToGrid(geom, 0.0000001)
  ```
  לקיבוע דיוק של 7 ספרות עשרוניות (כ-1.1 סנטימטר). פעולה זו תחסוך עד 30% מנפח תעבורת הרשת והזיכרון בעת סנכרון דרך `pglogical` ובעת טעינה ל-UI.

---

## 4. ארכיטקטורת הנתונים המרחבית (Spatial Data Model)

המערכת תממש מודל היברידי, המשלב עמודות SQL רלציוניות להיררכיה, PostGIS לטופולוגיה, ו-JSONB לתכונות.

### 4.1 סכימת ליבה ושיוך בלעדי (Exclusive Belongs-To)
כדי ליישר קו עם ארכיטקטורת ה-Mesh (סעיף 4.2 במסמך המקור) ולמנוע יצירת "נתונים יתומים", ניישם אכיפה קפדנית ברמת מסד הנתונים:

```sql
CREATE TABLE entities (
    entity_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    mission_id UUID REFERENCES missions(id),
    infra_id UUID REFERENCES infra(id),
    parent_entity_id UUID REFERENCES entities(entity_id), -- מאפשר קישור נקודה לפוליגון
    entity_type TEXT NOT NULL,
    geom GEOMETRY(Geometry, 4326),
    properties JSONB DEFAULT '{}',
    version INT DEFAULT 1,
    mission_change_seq BIGINT DEFAULT 0,
    is_deleted BOOLEAN DEFAULT FALSE,
    last_update_time TIMESTAMPTZ DEFAULT NOW(),
    origin_node TEXT,

    -- מונע חוסר עקביות בסנכרון בין מחשבים: שיוך למשימה או לתשתית (אך לא לשניהם יחד)
    CONSTRAINT belongs_to_one CHECK (
        (mission_id IS NOT NULL AND infra_id IS NULL) OR
        (mission_id IS NULL AND infra_id IS NOT NULL)
    )
);

CREATE INDEX idx_spatial_entities ON entities USING GIST (geom);
CREATE INDEX idx_entities_mission_delta ON entities (mission_id, mission_change_seq ASC);
```

> **הערה טכנית ל-pglogical וניהול קונפליקטים בנתק:** 
> 1. שימוש ב-UUIDv4 מונע התנגשויות (Primary Key Conflicts) לחלוטין כאשר מספר מחשבים יוצרים ישויות מרחביות במקביל בזמן נתק.
> 2. **הבדלה דטרמיניסטית בין כפילויות:** כאשר נוצרות בעת חזרה מנתק מספר ישויות בעלות אותו שם, לא ניתן להסתמך על חותמת זמן (Time) כיוון שזמן יצירה/סנכרון עלול להיות זהה לחלוטין. במקום זאת, יש להשתמש במיון לקסיקוגרפי מבוסס מזהה (UUID). לדוגמה ב-SQL:
> `ROW_NUMBER() OVER (PARTITION BY mission_id, name ORDER BY id::text ASC)`
### 4.2 אכיפת סמנטיקה כקו הגנה למערכת מבוזרת
במערכת מבוזרת עם Eventual Consistency, "מידע מורעל" (Poison Data) שנוצר עקב באג באחד המחשבים ישתכפל מיד לשאר ויקריס את כולם. מנוע ה-DB חייב לחסום זאת מראש:

```sql
ALTER TABLE entities
ADD CONSTRAINT enforce_spatial_integrity CHECK (
    -- מוודא שהגיאומטריה תואמת את ההגדרה הסמנטית.
    -- מעגל ונקודה חייבים שניהם להיות מסוג ST_Point.
    (entity_type IN ('point', 'circle') AND ST_GeometryType(geom) = 'ST_Point') OR
    (entity_type = 'polygon' AND ST_GeometryType(geom) = 'ST_Polygon')
);

ALTER TABLE entities
ADD CONSTRAINT enforce_circle_radius CHECK (
    CASE WHEN entity_type = 'circle' THEN
        (properties->>'radius')::NUMERIC > 0
    ELSE TRUE END
);
```

### 4.3 צינור נתונים ל-WebGL (High-Performance WebGL Pipeline)
כדי להוריד עומס ממחשבי 2014 המריצים את ה-UI, המפתחים לא יכתבו קוד JS שמסנן, מפרסר ומייצר אובייקטי מפה. מסד הנתונים ייצר Payload מצומצם (Shader-Ready) באמצעות View ייעודי. ה-DB ייצא את הנתונים כ-DTO מוכן באמצעות `jsonb_agg`.

- *(לבחינה עתידית)* נדרוש מהמפתחים להשתמש במפתחות קצרים ב-JSONB (לדוגמה `c` עבור צבע, `i` עבור אינדקס אייקון במקום מחרוזת ארוכה).

### 4.4 מדיניות מחיקה היררכית (Top-Down View Masking)
כדי למנוע "סערת כתיבה" (Write Storm) ועומס על רשת הסנכרון (pglogical), המערכת תמנע ממחיקה לוגית של ישויות בודדות בתוך משימה כאשר נמחקת המשימה כולה. במקום זאת, תיושם מדיניות מיסוך היררכי: מחיקה לוגית תתבצע אך ורק בטבלאות האב (`missions` / `infra`) ע"י עדכון עמודת `deleted_at`.  
השכבה המציגה (View) תבצע JOIN בזמן אמת ותסנן את כל הישויות השייכות לאב שנמחק. גישה זו מבטיחה עדכון אטומי של אלפי ישויות בפעולת כתיבה בודדת, תוך שמירה על ביצועי רשת מקסימליים ושרידות הנתונים לצורכי תחקור (Audit) לאחר סיום הסשן.

```sql
-- ה-View שמחליט מה המפה רואה
CREATE OR REPLACE VIEW v_active_entities AS
SELECT e.*
FROM entities e
JOIN missions m ON e.mission_id = m.id
WHERE m.deleted_at IS NULL; -- המחיקה של האב "מכבה" מיידית את כל הילדים
```

### 4.5 מבנה נתיבים ונקודות ניווט (Routes & Waypoints)
- **מבנה ישות נתיב:** נתיב מחזיק רשימה אטומית של מזהי נקודות (לדוגמה: `WptList: [id1, id2, id3]`). לא ניתן לשנות את רשימת הנקודות בנפרד מהנתיב עצמו. מבנה זה מכריח ישויות נלוות (כמו Alerts) המכילות Reference לנקודות בנתיב להתעדכן ללא קונפליקטים מול הרשימה האב, כי ה-Properties נערכים תמיד ביחד ברמת הנתיב.
- **אילוצי שמות:** לנקודות ניווט ולנתיבים קיימות מגבלות אורך תווים ומניעת תווים מיוחדים. במקרה של יצירת כפילויות, אם אין מספיק מקום בתווים לשרשור מזהה, מורידים את האות האחרונה בשם הקיים כדי לפנות מקום לסיומת (למשל ASAF1 ו-ASAF2).

---

## 5. ניהול מחזור חיים והפצת נתונים (Operational Flow)

### 5.1 הפצה בתחומי אחריות שונים
במערכת מבוזרת (Mesh) הרצה על חומרה מוגבלת, השאיפה ל-Reactive UI (מפה שמתעדכנת מיד כשמחשב אחר שינה נתון) טומנת בחובה סכנה של עומס רשת (Network Flooding) וקפיאות מסך (UI Jank). כדי למנוע טעינה מחדש של אלפי ישויות, המערכת שלנו תעבוד במודל של עדכוני דלתא (Delta Updates) מבוססי גרסאות, המנותקים לחלוטין משכבת האפליקציה (Node.js/React) לצורך יציבות:

- **ניהול גרסאות פנימי (Entity Versioning):** לכל ישות בטבלת `entities` נוספה עמודת `version` (מסוג Integer מהיר). לכל משימה בטבלת `missions` נוספה עמודת `last_change_seq` ("הדופק"). בעת כל שינוי בבסיס הנתונים (מקומי או כזה שהגיע דרך רפליקציית pglogical), טריגר ברמת ה-PostgreSQL מעלה אוטומטית את גרסת הישות, ומקפיץ אטומית את "הדופק" של המשימה האב. לא צריך לכתוב קוד ידנית בשביל שזה יקרה.
- **ניתוק שכבת ההתראות (The Sidecar Bridge):** כדי להימנע מבאגים של כתיבה כפולה (Dual-Write Problem) בהם האפליקציה מתעדכנת אך קורסת לפני שליחת התראה, האפליקציה לעולם לא תשלח הודעות ל-NATS באופן ישיר. הטריגר ב-PostgreSQL ישתמש בפקודת `LISTEN / NOTIFY` (אשר מבטיחה אטומיות בתוך הטרנזקציה). שירות חיצוני (Bridge) יאזין ל-Postgres ויעביר את התראת ה"דופק" ל-NATS. במקרה של קריסת השירות, הוא פשוט יעלה מחדש, כאשר שלמות הנתונים מובטחת ב-DB.
- **עדכון הדלתא ב-Client באמצעות שעון וקטורי דינמי:** כאשר ה-Frontend (OpenLayers) מקבל התראה ב-NATS שה-Sequence השתנה, הוא אינו מסתמך על מונה סקאלרי גלובלי (שעלול לבלבל בין מחשבים בנתק), אלא מעביר לשרת את השעון הוקטורי הדינמי שלו כפרמטר JSONB (`$2`, לדוגמה `{"pc1": 10, "pc2": 4}`). השאילתה ב-Postgres מושכת בצורה ממוקדת אך ורק את הישויות שהתחדשו מול כל צומת:
  ```sql
  SELECT * 
  FROM entities e
  WHERE e.mission_id = $1 
    AND e.mission_change_seq > COALESCE(($2 ->> e.origin_node)::bigint, 0)
  ORDER BY e.mission_change_seq ASC;
  ```
  *(הערה: מבנה זה מאפשר דינמיות מלאה לכל כמות מחשבים ומבטיח כי אם צומת חדש נוצר או התגלה לראשונה, `COALESCE` יחזיר 0 וכל ישויותיו יימשכו במלואן).*  
  גישה זו מבטיחה שעל גבי רשת ה-Mesh יעברו רק הנתונים של הישויות הספציפיות שהשתנו, וה-WebGL יבצע רק עדכון מקומי בזיכרון.

#### 5.1.1 מניעת כפילויות בהפצה (Distributed Deduplication)
בארכיטקטורת Mesh שבה מספר צמתים (Nodes) מריצים את שירות ה-Bridge במקביל, נוצר סיכון להצפת ה-NATS בהתראות כפולות עקב רפליקציית הנתונים (כל צומת יזהה שינוי וינסה להתריע). כדי למנוע "רעש" ברשת ועיבוד מיותר ב-UI, המערכת תממש מנגנון Deduplication מובנה ברמת ה-NATS JetStream:
1. **מזהה דטרמיניסטי (`Nats-Msg-Id`):** שירות ה-Bridge לא ייצר מזהה הודעה אקראי. במקום זאת, הוא יבנה Header בשם `Nats-Msg-Id` המורכב מצירוף של `EntityID` ו-`Version`.
2. **חלון זמן לחסימה (Duplicate Window):** ה-Stream ב-NATS יוגדר עם חלון זמן של 2–5 שניות. ברגע שהודעה עם מזהה מסוים התקבלה מצומת א', כל הודעה זהה שתגיע מצומת ב' (כתוצאה מהרפליקציה) תיחסם אוטומטית ע"י ה-Broker ולא תופץ לצרכנים.
3. **יתירות (Idempotency):** גישה זו מבטיחה שגם במצב של High Availability, ה-Frontend יקבל עדכון בודד עבור כל שינוי גיאומטרי, תוך שמירה על רוחב פס מינימלי ברשת ה-LAN.

#### 5.1.2 הבטחת מסירה ומניעת אובדן הודעות (Reliable Bridge)
מנגנון ה-`LISTEN / NOTIFY` של PostgreSQL הוא אטומי לטרנזקציה אך אינו מבטיח הגעה (Persistence) אם ה-Sidecar אינו מאזין באותו רגע. במידה והוא קרס, יכולות להיעלם הודעות. בעיה זו ניתן לפתור באופן הבא - ננצל את יכולות ה-Persistence של NATS JetStream כדי לדעת איפה הוא עצר:
- **שאילתת Last Sequence:** בעת עליית ה-Bridge, הוא יבצע שאילתת `LastValue` ל-Stream (באמצעות ה-Subject הרלוונטי). המטא-דאטה של ההודעה האחרונה ב-NATS תכיל את ה-`last_change_seq` האחרון שנשלח בהצלחה מה-DB.
- **השלמת פערים (Self-Healing):** אם ה-Sequence ב-DB גבוה מה-Sequence האחרון ב-NATS, ה-Bridge ימשוך את הדלתא מה-DB וישלים אותה ב-Stream לפני שהוא חוזר להאזין ל-NOTIFY.
- **יתרון ארכיטקטוני:** זה הופך את ה-Bridge ל-Stateless לחלוטין. ניתן להחליף את ה-Sidecar, לשדרג אותו או לאתחל אותו מבלי לחשוש מאיבוד סנכרון – כל עוד ה-NATS וה-DB שרדו, המידע על "איפה עצרנו" זמין תמיד.

### 5.2 ניהול מחזור חיים: מחיקה היררכית היברידית (Hybrid Deletion)
מחיקת נתונים בסביבת Mesh Active-Active היא הפעולה המסוכנת ביותר ועלולה לגרום להופעת "נתוני זומבי" (Zombie Data) בעת ניתוקי רשת, או לסערת כתיבה (Write Storm) בעת מחיקה גורפת. כדי להתמודד עם כך, מערכת C2 זו מיישמת מודל מחיקה היברידי המבדיל בין פעולות המפעיל:
- **מחיקת ישות בודדת (Tombstones):** כאשר מפעיל מוחק מטרה או רחפן ספציפי מתוך משימה פעילה, ביצוע DELETE פיזי ימנע ממנגנון ה-Delta Update שלנו להודיע ל-UI על המחיקה. לכן, מחיקה כזו מבוצעת כמחיקה לוגית באמצעות הפיכת דגל `is_deleted = true` (רשומת מצבה / Tombstone). הפעולה נרשמת כעדכון רגיל: ה-version של הישות עולה, ה-UI שואב את רשומת ה-Tombstone דרך שאילתת הדלתא הרגילה, מזהה את הדגל, ומסיר את הישות מה-Source של OpenLayers.
- **מחיקת משימה/תשתית שלמה (View Masking):** כאשר מפעיל סוגר משימה או מוחק שכבת תשתית המכילה אלפי ישויות, סימון Tombstone על כל ישות בנפרד ייצר עומס קריטי על המעבד ויחנוק את הרפליקציה ב-pglogical. פעולה זו תתבצע על ידי עדכון עמודת `deleted_at` בשורת ה-Parent בלבד (בטבלת המשימות). ה-View המשמש לרנדור המפה (`v_active_entities`) יכיל לוגיקת סינון ש"מכבה" מיידית את כל הישויות השייכות לאב שנמחק, מבלי לכתוב דבר לטבלת הישויות עצמה.
- **איסוף זבל (Garbage Collection):** כדי למנוע תפיחת בסיס הנתונים לאורך הסשן, ירוץ בסביבה הקרקעית Background Worker פעם במספר דקות. תפקידו יהיה לבצע DELETE פיזי (Hard Delete) על כל רשומת Tombstone או משימה שנמחקה לוגית לפני יותר מ-10 דקות (זמן המאפשר לכל צמתי ה-Mesh להסתנכרן ולקבל את פקודת המחיקה ב-UI). בסביבת הקצה המידע גם ככה נמחק בסוף משימה.

### 5.3 אופרציות כבדות: שכפול עמוק בתוך המאגר - Atomic Cloning
משימות מורכבות (אלפי ישויות) דורשות לעיתים שכפול (Mission to Template). ביצוע הפעולה באפליקציה (Read & Write) יגרום לקריסת ה-Sidecar או ה-UI עקב Latency.
- **השיטה:** שימוש ב-CTE (Common Table Expressions) ישירות ב-SQL לביצוע Deep Copy.
- **ביצועים:** כל התהליך מתרחש בתוך הזיכרון של Postgres בפעולה אטומית אחת. המערכת מייצרת UUID-ים חדשים בתוך השאילתה, והטריגרים של הסנכרון (pglogical) וה"דופק" (Notify) מופעלים אוטומטית כתוצאה מכך. מידע לא יוצא ממחשב הרכב החוצה ובחזרה.
- במידת הצורך, ניתן לשקול ביצוע מיזוג משימות (או לפחות חלק גדול מהתהליך) באותו האופן ולחסוך בלוגיקה מורכבת בשרת.

---

## 6. ניהול גרסאות נתונים ותאימות (Schema & Data Versioning)

במערכת מבוזרת שבה משימות נשמרות כקבצים או נטענות מגרסאות תוכנה שונות, לא ניתן להניח שה-Payload ב-JSONB (`properties`) תמיד יתאים לקוד ה-UI העדכני. כדי למנוע קריסות רינדור, ייושמו העקרונות הבאים:
- **טיפול בישויות לא מוכרות (Future/Legacy Support):** כדי לאפשר טעינת משימות מגרסאות שונות, עמודת `entity_type` תישמר כ-`TEXT`. אכיפת הסוגים תתבצע ברובד התצוגה (View-Level) בלבד. ישויות שסוגן אינו מוכר לגרסה הנוכחית יישמרו בבסיס הנתונים לצרכי תחקור אך יסוננו אוטומטית משאילתות הרינדור של המפה, כדי למנוע קריסות בשכבת ה-UI.
- **סימון גרסת מקור (Schema Versioning):** לכל ישות (Entity) תתווסף עמודת אינטגר `schema_version`. עמודה זו מייצגת את גרסת מבנה ה-JSON שאליו התכוון הכותב בזמן היצירה. בטעינת משימה ישנה, המערכת תזהה את הגרסה ותדע האם נדרשת פעולת "תיקון" (Migration) או שימוש במפענח (Decoder) ישן.
- **הגנה בטרנספורמציה (View-Level Adapter):** ה-View המשמש את המפה (`v_map_render_layer`) יכלול פונקציות `COALESCE` בתוך ה-JSONB. במידה ושדה קריטי לרינדור (כגון `opacity` או `line_dash`) חסר בנתונים הישנים, ה-DB יזריק ערך ברירת מחדל (Default Value) בזמן השליפה, מבלי לשנות את נתוני המקור.
- **מדיניות אי-חסימה (Non-Breaking Ingest):** בניגוד לעמודות רלציוניות, שינויים בתוך ה-`properties` לעולם לא יגרמו לכישלון טעינה (Drop). ישויות שאינן עוברות וולידציה בגרסה הנוכחית יסומנו ב-Flag פנימי כ-"Invalid" אך יישמרו במסד הנתונים, כדי למנוע איבוד מידע מבצעי יקר בתרחישי קצה.
- **שדרוג אטומי (On-Load Migration):** בעת ייבוא משימה מגרסה קודמת, יופעל Service ייעודי המבצע UPDATE אטומי בתוך ה-DB שמביא את כל ה-JSONs לסטנדרט הנוכחי (למשל: שינוי שמות מפתחות או המרת יחידות מידה).

---

## 7. התאמה לתצורת ענן וריבוי משתמשים (Cloud & Backend Scaling)

בעוד שהארכיטקטורה המקורית תוכננה לשרידות מקסימלית על חומרת קצה (Edge), המעבר לסביבת ענן/מרכזית מחייב התאמות לטיפול ב-High Concurrency (עבודה מקבילה של מאות משתמשים) ונפחי נתונים הגדלים ליניארית:

### 7.1 מעבר מ-Push מבוסס Trigger ל-CDC
בענן, מנגנון ה-`LISTEN / NOTIFY` עלול להפוך לצוואר בקבוק במעבד.
- **הפתרון:** שימוש ב-Change Data Capture (כגון Debezium) הקורא ישירות את ה-Write Ahead Log (WAL) של ה-DB.
- **היתרון:** ה-Bridge הופך להיות צרכן חיצוני שאינו מעמיס על פעולות הכתיבה ב-DB, ומבטיח שההודעות ל-MQ יישלחו גם תחת עומס טרנזקציות קיצוני.

### 7.2 אופטימיזציית שאילתות בנפחי ענק
השימוש ב-View המבצע JOIN בזמן אמת (סעיף 4.4) עלול להפוך לאיטי כאשר בטבלת ה-Entities ישנם מיליוני רשומות.
- **הפתרון:** יישום Declarative Partitioning על בסיס זמן (כלומר לייצר טבלה חדשה כל כמה חודשים/שנה).
- **הביצוע:** ה-DB יפצל פיזית את הנתונים לטבלאות קטנות יותר. שאילתת המפה תסרוק רק את ה-Partition הרלוונטי למשימה הפעילה, מה שישמור על זמן תגובה של מילי-שניות בודדות ללא קשר לגודל המאגר הכולל.
- יישום ה-Partitioning יתבצע אך ורק בתצורת הענן שבה נצברים נתונים היסטוריים רב-שנתיים. בסביבת הקצה (מחשב הרכב), המערכת תמשיך לעבוד עם טבלה שטוחה כדי לחסוך ב-Overhead ניהולי על חשבון ה-CPU המוגבל.

### 7.3 ניהול קונפליקטים ו-Connection Pooling
בתצורת ענן נשתמש ב-Quorum כפי שתשתיות מספקים לנו, ולא צריך לדאוג לתשתית הזו ברמת הרכב הבודד.

---

## 8. סיכום היתרונות של הארכיטקטורה המרחבית
- **שרידות תפעולית (Autonomous Edge Survival):** ביזור מלא המבוסס על UUID ו-Conflict Resolution של pglogical מאפשר המשכיות מבצעית מלאה בנתק רשת (Split-Brain).
- **עקביות סמנטית (Hardened Schema):** השימוש ב-Constraints וב-JSON Schema Enforcement הופך את בסיס הנתונים ל"שומר הסף" של המערכת. פגיעה בשלמות הנתונים נחסמת במקור, מה שמונע הפצת "מידע מורעל" (Poison Data) בשאר צמתי ה-Mesh.
- **תשתית "חסינת עתיד" (Forward Compatibility):** הפרדה מוחלטת בין טבלאות הליבה (Storage) לבין שכבת התצוגה (Views/Adapters) מאפשרת טעינת משימות מגרסאות שונות וביצוע Migration שקוף למשתמש, תוך שמירה על ביצועי שאילתות גבוהים.
- **ניתוק צימוד (Decoupling) והפצה אמינה:** מנגנון ה-Sidecar Bridge בשילוב NATS JetStream מבטיח מסירת הודעות (Reliable Delivery) ואטומיות בין כתיבת הנתון להפצתו, ללא "כתיבה כפולה" (Dual-Write) המסכנת את עקביות המערכת.

---

## 9. נקודות פתוחות הנדסית
- **אתחול הרחבות (Extensions):** בעת הקמת ה-Docker של ה-DB עם pglogical, יש לוודא ש-postgis נטען ומוגדר לפני שמגדירים את ה-Replication. אם ה-Mesh ינסה להעביר עמודת גיאומטריה למחשב שעוד לא התקין את ה-Extension, ה-Worker יקרוס.  
  *הפתרון הפרקטי ביותר:* לבנות Custom Docker Image שבו ה-PostGIS וה-pglogical כבר מותקנים ומוגדרים ב-`postgresql.conf`, והאתחול לא מוגדר ב-Runtime.
