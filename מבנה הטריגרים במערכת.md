### מושגי יסוד בקצרה

- **מה זה Trigger (טריגר)?** זוהי פונקציה אוטומטית המופעלת ברמת בסיס הנתונים ברגע שמתרחש אירוע מוגדר (למשל: עדכון, הוספה או מחיקה של שורה בטבלה).
    
- **מה הם `NEW` ו-`OLD`?** בזמן שהטריגר רץ על שורה ששונתה, `OLD` מייצג את הנתונים שהיו בשורה **לפני** השינוי, ו-`NEW` מייצג את הנתונים החדשים שנרצה לשמור.
    
- **מה זה `pg_notify`?** מנגנון Pub/Sub פנימי של בסיס הנתונים השולח הודעות קלות בזמן אמת לכל שירות/קוד חיצוני שמאזין לו.

- **סוגי טריגרים בסביבה מבוזרת (pglogical):**
  - **DEFAULT (ORIGIN):** טריגר המופעל אך ורק כאשר אפליקציה מקומית מבצעת כתיבה ישירה על אותו צומת (Node).
  - **REPLICA:** טריגר המופעל אך ורק כאשר מגיעים שינויים ממקור מרוחק (דרך הרפליקציה של `pglogical`). נשאר "שקט" עבור כתיבות של האפליקציה המקומית.
  - **ALWAYS:** טריגר המופעל על כל שינוי, ללא קשר למקורו (הן מקומי והן דרך הרשת).
### זרימת התהליך בעת שינוי מידע (Execution Flow)

נניח שמשתמש או שירות חיצוני מעדכן שדה (למשל `properties` או `geometry`) בטבלת היישויות והנתיבים (**`entities`**). הנה מה שקורה ברקע, שלב אחר שלב:

#### 1. גיבוי ה-OLD, אכיפת 10 גרסאות וקידום מונוטוני (`trg_route_lww_backup`)

ברגע שנשלחת פקודת `UPDATE` או `INSERT`, קופץ הטריגר הראשון שרץ **לפני** שהשינוי נשמר סופית בטבלה (`BEFORE INSERT OR UPDATE`):

- **שמירת ה-OLD בגיבוי (בפקודת `UPDATE`):** הטריגר לוקח את כל המידע שהיה קיים בשורה (`OLD.properties`, `OLD.geometry`, `OLD.version` וכו') ומכניס אותם כגרסה חדשה בטבלת `route_backups`.
    
- **אכיפת תקרת 10 גרסאות (Pruning):** הטריגר מוחק אוטומטית מ-`route_backups` גרסאות ישנות של ה-`route_id` הנוכחי שחרגו מתקרת 10 המגובים. _(הערה: מומלץ בהמשך להעביר ניקוי זה לתהליך רקע/Cron בלתי-תלוי כדי לא לעכב את פקודת ה-UPDATE)._
    
- **קידום הגרסה והמונה המשימתי:**
    
    - הטריגר מעלה את מספר הגרסה של היישות עצמה (`NEW.version = OLD.version + 1`).
        
    - **קידום מונה משימתי אטומי:** הטריגר מקדם אטומית את המונה בטבלת המשימות עבור המשימה הנוכחית (`missions.last_change_seq`), תוך שימוש ב-Row Lock קצר במהלך הטרנזקציה:
        
        ```sql
        UPDATE missions
        SET last_change_seq = COALESCE(last_change_seq, 0) + 1
        WHERE id = NEW.mission_id
        RETURNING last_change_seq INTO v_next_seq;
        
        NEW.mission_change_seq := v_next_seq;
        ```
        
        _הסבר ארכיטקטוני:_ קידום המונה ברמת שורת המשימה מבטיח רצף מספרי מושלם וסטרילי (ללא פערים/Gaps וללא Out-of-Order Commits) בתוך אותה משימה, ומאפשר סנכרון דלתה פשוט, אמין ודטרמיניסטי.
#### 2. שליחת ההתראה בזמן אמת (`trg_entities_notify`)

לאחר שהנתונים החדשים והגרסאות נשמרו בטבלה, קופץ הטריגר האחרון (`AFTER INSERT OR UPDATE`) שמפיץ את הבשורה מחוץ לבסיס הנתונים:

- **בניית ה-Payload (הודעה רזה):** הטריגר אוסף את הנתונים העדכניים: מזהה המשימה (`mission_id`), מזהה היישות (`entity_id`), הגרסה החדשה (`version`), מספר השינוי המשימתי (`last_change_seq`) והרכיב שביצע את השינוי (`origin_node`).
    
    > **דגש ארכיטקטוני:** כדי להימנע ממגבלת הגודל של `pg_notify` (עד 8KB), ה-Payload אינו מכיל שדות כבדים (כמו `geometry` או `properties` נפחיים) אלא **מזהים וגרסאות בלבד**.
    
- **שידור ההתראה (`pg_notify`):** הטריגר אורז את הפרטים למבנה JSON ושולח אותם לערוץ התקשורת `entity_changes`. אפליקציות ושרתים המאזינים לערוץ מקבלים את ההודעה מיד ויודעים איזה מפה או תצוגה יש לרענן בזמן אמת.
    

### מה קורה כשיש שינוי ישיר במשימה עצמה? (`trg_missions_notify`)

אם השינוי אינו ביישות אלא במאפיינים של המשימה עצמה (למשל: שינוי שם המשימה או מחיקתה בטבלת `missions`):

- קופץ טריגר ייעודי על טבלת המשימות שמזהה שהשם או סטטוס המחיקה השתנו.
    
- הטריגר מפיץ הודעה נפרדת בערוץ `mission_changes` עם הפרטים של המשימה והפעולה שבוצעה (`created` או `updated`) כדי שכל המערכות יידעו על השינוי במפתח המשימה.