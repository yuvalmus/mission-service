**טבלת ישויות (Entities)** טבלה זו מייצגת את הישות המרכזית במערכת, המקשרת בין משימות לתשתיות ומחזיקה מידע גיאוגרפי ומטא-דאטא.

- **id** (`uuid`): מפתח ראשי (Primary Key) המזהה את הישות באופן ייחודי.
    
- **missionId** (`uuid`): מפתח זר (Foreign Key) המפנה לשדה `id` בטבלת **משימות**.
    
- **infraId** (`uuid`): מפתח זר (Foreign Key) המפנה לשדה `id` בטבלת **תשתיות**.
    
- **properties** (`JSONB`): שדה לטיפול בנתונים גמישים במבנה JSON דינמי.
    
- **geometry** (`GeoJson`): מפתח גיאוגרפי המאחסן מידע מרחבי (כגון קואורדינטות, פוליגונים וכדומה).
    
- **lastUpdateTime** (`Time`): חותמת זמן המציינת מתי הישות עודכנה לאחרונה.
    
- **isDeleted** (`Boolean`): דגל שמציין מחיקה רכה (Soft Delete) – האם הישות מבוטלת/מחוקה.
    

**טבלת משימות (Missions)** טבלה זו מייצגת משימה במערכת.

- **id** (`uuid`): מפתח ראשי מזהה עבור המשימה.
    
- **name** (`string`): שם המשימה.
    
- **metadata** (`JSONB`): נתונים נוספים או קונפיגורציה של המשימה במבנה JSON דינמי.
    
- **version** (`int`): מספר גרסה לצורך מעקב או לניהול נעילות מתחרות (Optimistic Locking).

- **last_change_seq** (`bigint`): מונה רציף מבוקר של שינויים ברמת המשימה, המשמש לסנכרון דלתה מדויק ללא פערים.

    

**טבלת תשתיות (Infrastructures)** טבלה זו מייצגת תשתית במערכת (כגון מכלול או שייכות ארגונית).

- **id** (`uuid`): מפתח ראשי מזהה עבור התשתית.
    
- **squadron** (`string`): שם או מזהה הטייסת/היחידה אליה שייכת התשתית.
    
- **metadata** (`JSONB`): נתונים נוספים על התשתית במבנה JSON דינמי.
    
- **version** (`int`): מספר גרסה המיועד לניהול גרסאות או נעילת עדכונים.
    

**קשרים ביניהן (Relationships)**

- **משימות <-> ישויות**: קשר של אחד-לרבים (1-to-N). משימה אחת בטבלת **משימות** יכולה להיות מקושרת למספר **ישויות**, אך כל ישות משויכת למשימה אחת (דרך `missionId`).
    
- **תשתיות <-> ישויות**: קשר של אחד-לרבים (1-to-N). תשתית אחת בטבלת **תשתיות** יכולה להיות מקושרת למספר **ישויות**, אך כל ישות משויכת לתשתית אחת (דרך `infraId`).